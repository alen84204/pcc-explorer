# PCC Explorer 問題追蹤清單

更新時間：2026-05-06（Asia/Taipei）

## 已確認問題

1. `data.json` 被當成清洗後資料使用，導致無法直接查看 API 原始 `records`。
2. 每日資料檔未帶日期檔名，會覆蓋前一天內容，不利追溯與稽核。
3. 目前需要「原始檔」與「清洗檔」分離保存，避免資料語意混淆。
4. 逐筆呼叫 `tender_api_url` 取得 `category` 時，容易觸發 API 限流（`429`）或造成執行逾時。
5. 一旦限流，流程可能卡住或只產生部分輸出（例如只有 raw 更新、clean 未完成）。
6. `採購性質` 目前含推論邏輯，不完全是官方原始欄位，合規使用需明確標示來源與轉換方式。
7. 先前 README 日期更新曾有正則命中問題（粗體標記干擾），雖已修正，但需持續驗證。

## 已有共識（方向）

1. 每日從 PCC API 抓回來的資料應優先以「原始格式」保存。
2. 清洗/轉換後資料應另存獨立檔案，供前端或分析使用。
3. 後續檔名應包含日期，例如：
   - `listbydate_YYYYMMDD.json`（raw）
   - `dashboard_data_YYYYMMDD.json`（clean）
4. 對 `tender_api_url` 的逐筆查詢應採可開關模式，預設關閉，避免日常排程被限流拖垮。
5. 標案分類來源補充（待統一處理）：
   - `https://schema.gov.tw/en/code/detail/6576`
   - `https://data.gov.tw/dataset/6576`

## 待執行項目（你回來後可直接接）

1. 修改 `pcc_crawler.py` 輸出策略：
   - raw 固定檔 + 日期檔雙寫
   - clean 固定檔 + 日期檔雙寫
2. 明確文件化資料契約：
   - `data.json` 是否保留 raw
   - `dashboard_data.json` 是否為 clean
   - 日期檔命名規則
3. 加入限流保護：
   - `tender_api_url` 查詢開關
   - 逾時/重試策略與失敗時退回機制
4. 補一份最小驗證清單：
   - 檔案是否完整產出
   - raw/clean 筆數是否一致
   - README 日期是否更新成功

## 長期待辦（延後，預計半年後再啟動）

1. 官方資料來源完整盤點與技術探索（`web.pcc.gov.tw`、`data.gov.tw`、相關官方下載資源）。
2. 釐清是否存在官方可穩定機器介接之 API/資料管道，並建立正式資料契約。
3. 建立官方來源版本的下載、驗證、落地與更新策略（含法遵與稽核需求）。
4. 在技術成熟前，現階段維持使用第三方來源（目前為 `pcc-api.openfun.app`），並保留切換到官方來源的設計空間。
